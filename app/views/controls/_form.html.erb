<%= form_for(@control) do |f| %>
  <% if @control.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@control.errors.count, "error") %> prohibited this control from being saved:</h2>

      <ul>
      <% @control.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="field">
      <%= f.label :connections %><br>
      <%= f.select :name, Control.connections.keys {|c| [ c.humanize.capitalize, c ] } %>
    </div>
    <div class="field">
     <%= f.label :friendly_name %><br>
     <%= f.text_field :friendly_name %>
    </div>
    <div class="actions">
     <%= f.submit %>
    </div>
<% end %>
